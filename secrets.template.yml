# ESP-NOW Pairing Secret Configuration (TEMPLATE)
#
# Copy this file to secrets.local.yml and set a strong random secret.
# Keep secrets.local.yml out of git.
#
# Generate a secret:
#   openssl rand -hex 16
#
# You can also pass the secret via:
#   ./scripts/build_app.sh <app> <type> --secret <32_hex_chars>
# or environment variable:
#   ESPNOW_PAIRING_SECRET=<32_hex_chars> ./scripts/build_app.sh <app> <type>

espnow_pairing_secret: "YOUR_SECRET_HERE"
